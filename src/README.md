# Tickets Portal GSB (MVP)

Portal de tickets sin Power Pages, usando **Next.js + Entra/B2C + Graph + SharePoint**.

## Requisitos locales
- Node.js 18 o superior
- Navegador web

## Variables de entorno
Copiar `.env.example` a `.env.local` y completar luego (más adelante configuramos B2C/Entra y SharePoint):

``
## Instalar dependencias
> Lo haremos en el **PASO 5.2**, con:

## Correr en local (después de instalar)

Abrir: http://localhost:3000

## Estructura básica

## Próximos pasos
1. PASO 5.2: instalar dependencias y probar `/api/hello`.
2. Configurar autenticación (B2C/Entra) y `/api/me`.
3. Conectar con SharePoint (lista “Tickets” y doc lib “Ticket GSB”).
4. Integrar con Power Automate por HTTP.

